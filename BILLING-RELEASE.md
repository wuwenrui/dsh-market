# 内置费用入口发行说明

本次市场版本为 `1.46.1-lawyer.2`。在既有受管实现 `267fa70757a15c22aac0d2b1e67eedfbed7d3ecd` 上，将模型服务“账户与充值”改为可取消的 `lawyer-billing:open` 界面事件。插件缺失时显示安装/重启提示；市场不创建订单，不读取模型令牌，不回退外链。

发行包含此前已随 LawyerDesk 受管产品交付、但尚未合入本仓 main 的受管基础。不能只把本次小改动移植到尚不包含受管实现的社区型 main。`ManagedMarket.tsx` 的字典已独立拆分，旧中英文翻译逐值保持不变，组件低于通用500行硬线。

## 已验证

- 类型检查、正常客户端构建、71个测试文件中的1439项测试全部通过。
- 配套产品完整真实 Web 验收通过全部五个预设及费用/人工充值入口。修复仅是等待实际预设选择 RPC 成功后再发送测试消息；保留原 bash 工具断言，没有修改宿主。
- 本次插件入口改动不涉及计费或审核；业务由独立费用插件和模型站共享人工充值流程承担。

## 已批准的历史结构例外

从 main 检查完整受管发行差异时，通用 `repo-dev` 行数门禁仍报告14个超长文件。其中13个旧源码/测试文件与既有受管基线逐字未变；另一个是正常构建生成的 `client/client.js`。它们不是本次充值新增的大型模块。

用户已明确选择“保留历史结构，继续发布”，仅针对本次发行接受这批历史结构，不在发版时进行无关大重构。没有修改门禁规则、注册放宽基线或使用 `--no-verify`；不将静态门禁失败记录为通过。类型、测试、构建、签名和实际入口验收仍必须通过。
